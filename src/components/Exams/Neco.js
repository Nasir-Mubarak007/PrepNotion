import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Neco = () => {
  return (
    <View>
      <Text>Neco</Text>
    </View>
  );
};

export default Neco;

const styles = StyleSheet.create({});
