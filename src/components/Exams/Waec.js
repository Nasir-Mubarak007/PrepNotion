import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Waec = () => {
  return (
    <View>
      <Text>Waec</Text>
    </View>
  );
};

export default Waec;

const styles = StyleSheet.create({});
